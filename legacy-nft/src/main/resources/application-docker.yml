spring:
  datasource:
    url: jdbc:mysql://mysql:3306/legacy_nft?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: app
    password: apppass
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
